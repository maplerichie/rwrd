export interface Redemption {
  id: string;
  subscription_id: string;
  customer_id: string;
  merchant_id: string;
  product_id: string;
  redeemed_at: string;
  amount: number;
  transaction_id: string;
} 